import Individual from "./IndividualClass.js";

let ind1 = new Individual(-150, 0, 0.5, "standard Tamatoken.png");

class Colony {
  constructor(colonySize, c) {
    this.colonySize = colonySize;
    this.colony = [c];
  }

  // // individuals() {
  // //   if(ind1.body()){
  // //   colony.push("token"); //array into which individuals are pushed (?)
  // //   }S
  // // }
  // displayColony(){
  //   if(food.hitTest()){
  //     this.x = this.x + 50;
  //   }
  // }
}
